# Production Hideout

Entry scene: `res://maps/hideout/hideout.tscn`.
The original F6 rehearsal under `tools/live_lobby_preview/` remains isolated.

See [migration behavior, verification and follow-up](../../docs/HIDEOUT_MIGRATION.md).
This production copy supports the first shared-room and match-return test.
Atomic squad travel, authenticated access policies and world records are separate
follow-up work; the UI does not label them as working.
