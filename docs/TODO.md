# One Gun — TODO / Known Gaps

> Originally compiled from a full read-through and updated through 2026-08-26. Items are grouped by kind, not by priority — see the "Recommended Priorities" section at the bottom for suggested order.

## In Progress

- **Quality repair pass (2026-09-07):** solid-cover gun/melee validation, online tracer retirement, optimized bot/bat assets, owned asynchronous map loading, clean rejection/despawn/menu lifetimes, Trippy intro bounds, Neon callouts, full-cycle Hat/ADS clearance and optional ping/cover feedback are implemented. See [QUALITY_PASS_2026-09-07.md](QUALITY_PASS_2026-09-07.md). Release work still includes weak-laptop frame pacing, two-machine latency/reconnect behavior, dark-skin readability and physical-controller animation/transition feel. Protocol 3 needs coordinated client/server/coordinator deployment; the existing live protocol-2 gate is historical evidence. **Hideout stays local and unintegrated.**


- **Winners Circle Ready/Return input repaired** (2026-09-03): the full-screen results overlay now owns modal input, preventing an eliminated player's still-active spectator controller from consuming LMB/RMB or gamepad actions before the visible Ready and Return All buttons receive them. Dedicated-session lobby moderators receive Return All and route it to authoritative peer 1 with controller-only server validation. The permanent Winners Circle regression reproduces real viewport clicks with a background spectator present.

- **Aim/character/Playpen/Friends reliability batch complete** (2026-09-02): human bullets now use one viewport-local center-ray direction/origin pair from the `Camera3D` that actually renders the player's SubViewport, with exact `project_position()` placement at the stable actor/aim-pivot depth. The animated shoulder-offset muzzle is visual-only and cannot influence a human projectile's spawn. Bodies enter the physics world already at that point, remain hidden through launch, and reset interpolation before their first visible frame; the host validates an owner's submitted pair and never mixes it with a delayed host camera transform. The bright 0.1m projectile retains a precise 0.025m collision core. The focused regression deliberately offsets the non-rendering source camera and checks the spawned body both immediately and later in its flight; a permanent GPU-backed Neon Circuit regression also shifts the visual muzzle two meters sideways and verifies solo, Player A, and Player B remain within 0.5px of their rendered crosshair across four physics frames. Character wrappers stop imported autoplay, expose the one authoritative animation player even with animated Hats equipped, update grounded animation after physics in every movement interruption branch, and force/publish Idle on respawn. All six developer-gift characters now use current 512px in-engine Idle screenshots for their Locker/profile tiles. Playpen station guns remain frozen at identical deterministic transforms until pickup and use a latency-compatible host distance tolerance. Friends-window Join and invite-toast Accept defer network startup, while the existing Online Play controller is the sole home-screen success transition, eliminating the duplicate scene change that crashed both paths. Combat, social, Locker, all-character 33-animation, and Playpen validations pass. Remaining release QA is a real two-machine invitation/join pass plus a hands-on 1080p Low aim/repeated-switch/Playpen run on the weaker laptop.

- **Developer-gift size/shared-hitbox normalization complete** (2026-08-29): Gold Fish Bag Man, Eye Wizard, Mr. Mushroom, Mr. Poop, Mr. Salt, and Spooky Witch are grounded and normalized to the 2.6453-unit Male/Female cat midpoint while retaining the exact same `player.tscn` capsule as both public cats. A permanent live-skinned-bounds regression covers all eight registered models, and the home showcase uses those validated actor-space envelopes for reliable podium framing without runtime vertex scans. Remaining QA is a hands-on six-character combat pass and repeated switching at 1080p Low on the weaker laptop.

- **City overtime visibility repaired** (2026-08-08): restored the accidentally empty City navigation bake (400 polygons), required the City OT regression to verify that authored bake, and added an emergency projected-floor fire surface so lethal OT ground cannot become invisible if a future map bake is missing. The through-cover survivor pulse now uses a scale-independent character-only Fresnel rim instead of the depth-free expanded shell that turned the 55.47x V2 visual and attached equipment into a red blob. Remaining validation is a hands-on Forward+ playtest of the rim thickness at typical City sightlines.

- **Mascot/weapon production packet in progress** (2026-07-16): `docs/design/ONE_GUN_CHARACTER_WEAPON_ULTRA_PACKET_CODEX.md`. Section 1 (audit + pipeline) approved; Section 2 (Blue Cat reference extraction + measured 1.4 m turnaround) delivered and awaiting approval — sheets in `art_src/mascots/blue_cat/renders/S2_*`, measurements/proposals in `art_src/mascots/blue_cat/ref/BLUECAT_TURNAROUND_NOTES.md`. See `docs/design/MASCOT_PIPELINE.md` for stage tracking and compatibility flags (1.4 m concept scale vs 2.33 m gameplay collider, reload duration baked in `gun.tscn`, index-based animation merging).

## Explicitly Unfinished (code says so)

- **In-match music is not wired up.** `audio_manager.gd`'s `MUSIC_PATHS["game"]` entry is commented out with an instruction comment to drop a file into `res://audio/` and uncomment it. Menu music works; matches are silent.
- **Several event SFX hooks remain unwired.** Gunfire, melee swings/hits, footsteps, UI sounds, and the generated confetti party-favor pop are live. Pickup/drop, disarm, and round start/end still have commented `AudioManager` placeholders with no final assets.
- **Main menu button stagger animation was gutted for debugging.** `main_menu.gd`'s `_stagger_page_buttons()` has a comment stating the slide/stagger animation was "stripped for debugging" — buttons fade in but no longer slide/stagger as apparently originally designed. Needs restoring or intentionally removing the dead code path.
- **Online transport polish remains.** The full match stack now includes teams, compatibility checks, scene-load recovery, late waiting/spectator roles, dedicated-server/Edgegap hosting, and loopback smoke coverage. A rendered public Edgegap client test now passes movement, pickups/combat, and lobby return; remaining work is latency smoothing/prediction, reconnect/host-migration policy, Steam transport integration, and a real two-machine/friend internet playtest. Dynamic per-match Edgegap servers now have verified `MM_*` bootstrap validation, one-time ticket admission, a deployed backend-only-token Cloudflare coordinator, automatic client endpoint migration, and a live two-client protocol-2 match gate. Production later needs authenticated lobby ownership rather than the anonymous development queue. The dedicated smoke suite now requires clean match-to-lobby teardown and rejects actionable `NetSync` cache errors; departing Playpen actors retain a short synchronization grace.
- **Player 2's name is session-only.** `GameConfig.player2_name` is explicitly commented as "session-only, not saved to disk — P2 may be a different guest each time." This looks intentional (P2 is a rotating local guest) but is worth confirming with design intent rather than assuming it's an oversight.

## Recently Added, Needs Follow-up

- **Branded launch + approved Barlow startup cinematic** (2026-08-29): client startup now replaces the default Godot boot image and application icon with temporary project-local One Gun branding, runs the approved five-word Barlow Condensed smooth-shot cinematic, holds on its One Gun title prompt, and fades into the existing main menu after deliberate input. The Windows client export no longer creates the console wrapper; dedicated `--server` startup still bypasses the cinematic. Automated production-scene handoff and Barlow render checks cover the authored flow. Before the next public upload, launch the exported EXE directly and through itch on the weaker laptop to confirm boot continuity, first-run shader behavior, input skipping, and stable memory after intro → menu transitions; replace the temporary splash/icon when final branding art is approved.

- **Gold Fish Bag Man gift character live** (2026-08-28): the supplied sub-1 MB GLB is registered as an entitlement-only character model, preserves its authored material and 65-bone rig, retargets every current gameplay/podium/round animation through the shared 33-bone core, carries animated Hat sockets, and appears with equipped visual cosmetics on home, gameplay, The Playpen, Locker previews, online peers, and every Winners Circle performer. Its shared wrapper corrects the authored mesh's reversed facing by 180 degrees, and the Playpen now carries the same model ID and sanitized cosmetic loadout used by normal match spawns. The public M/F selector and Prize Counter exclude it; losing/signing out of the entitlement restores the last public model. Male/Female rig regressions, Gold Fish orientation/animation/cosmetic checks, Playpen, home/game/Locker/Prize Counter/Winners Circle checks, and two-process cosmetic sync pass. Migration `20260903` is applied to the linked project, the local/remote ledger matches, and the follow-up dry run is up to date. A real gifted account passes the read-only ownership/RLS and actual Locker Preview/Equip-row checks even before hidden catalog metadata is available; the remaining hands-on check is a complete real-account match/Winners Circle.
- **Five developer-gift fixed-look characters integrated** (2026-08-29): Eye Wizard, Mr. Mushroom, Mr. Poop, Mr. Salt, and Spooky Witch now have normalized wrappers, stable entitlement model/item IDs, supplied portraits, authored-material protection, full 33-animation retarget coverage, gameplay hand/item/foot/head sockets, and shared rigid/skinned cosmetic binding. Male/Female remain the only public model choices; the five added characters are hidden, non-purchasable, absent from Prize Counter rotations, and appear in Locker Skins only for accounts that receive a trusted developer inventory grant. Mr. Mushroom uses a cap-aware elevated broad headwear profile; all 12 Hats pass on all eight registered models (96 combinations) in the Hat Fitter/runtime probes and Forward+ front/overhead sheets. Live validation cycles every added model through `player.tscn`, the networked Playpen actor, entitlement-driven home showcase podium, and Winners Circle performers with Hats and victory moves. Forward+ key-pose sheets cover Idle, Run, gun stance, melee, throw, and victory movement for every added rig. Migration `20260904` is applied to the linked project, the local/remote ledger matches, and the follow-up dry run is up to date. Remaining release QA is a hands-on six-character combat match, a real gifted-account pass, and repeated model/Hat switching on the weaker laptop.

- **Primary controller selection + full input audit** (2026-08-25): Player Settings → Controls now persists a Mouse & Keyboard (default) / Controller choice for the primary player, exposes complete P1/P2 controller remapping including right-stick axes, shows connected-pad status, and restores the saved device/bindings on Cancel. Runtime InputMap filtering prevents the inactive P1 device from also driving gameplay; splitscreen routes separate detected pads and refreshes after hot-plug. P1 and P2 now share complete A/B/X/Y, stick, trigger, bumper, sprint, and D-pad decoy defaults. Start/Menu pauses without colliding with B/Circle dash, Back/View owns the scoreboard, A/Cross and B/Circle provide explicit UI select/back, pause/settings cabinets take controller focus, spectators inherit the selected device, and pickup prompts show the actual remappable binding. The expanded disk-free validation passes every gameplay action, menu actions, device switching, remap buttons/axes, and split routing. Remaining follow-up is a hands-on Xbox/PlayStation-compatible pad pass (single-player, online, one-pad M&K+controller splitscreen, two-pad controller splitscreen, hot-plug, menus, and spectating) plus broader long-grid Player Hub navigation feel.

- **Controller/social/gameplay feedback batch** (2026-08-26): Classic OT now moves the final melee supply to map center and preserves loose pickups; All Gun OT clears its label and starts fire; Playpen validates safe spawns and recovers falling actors; ordinary combat vertical impulses are capped outside explicit launch states; Winners Circle input/focus and staged XP/Trophy-or-Token/reward presentation are fixed; the lobby Player Hub owns Locker/Prize Counter/Progression/Friends; all melee shares Frying Pan timing; Fast Hands adds a 50% swing-speed effect with unchanged stamina; Female V2 uses the Male master idle; network animation replication covers spectators; and both Bear Trap forms are 30% larger. Strict lobby/named-lobby/exit/Playpen/late-spectator/full-match suites, dedicated controller/guest combat, headless gameplay/UI validations, and the Forward+ render matrix pass. Remaining human checks are physical-controller latency/feel on a real multi-machine Tailscale session and repeated-transition profiling on the weaker laptop. Replace Fast Hands' generated pink capsule with the supplied final PNG when art arrives.

- **Authenticated friends/presence/lobby invites** (2026-08-26): Home and online-lobby Friends panels support exact-Account-Name requests, Accept/Deny, removal, accepted-friend online state, and named-Tailscale-lobby invitations. Direct table privileges are revoked; requester-bound RPCs and accepted-friend checks protect expiring presence/endpoints and short-lived invites. The Godot contract and disposable PostgreSQL social graph pass, migration `20260831_friends_presence_invites.sql` was applied successfully to the linked project, and the local/remote migration ledgers match through `20260831`. The backend is live. A two-real-account Tailscale UI/invite check remains as a hands-on release check.

- **Economy v3 + Official forfeit settlement** (2026-08-24): Official rewards pay fixed 250/200/150/100 placement Gun Tokens and 20–100 performance XP, use a gradual `150 + 2 × (level - 1)` Season curve plus flat 400-XP Career levels, and grant the first Official Classic win each UTC day 25 extra Season XP plus 100 Gun Tokens. The database owns the unique daily receipt. Quitters remain DNF, while one- or two-finisher matches settle from the locked three-plus starting roster. Remaining manual work is real-account three-machine normal/two-finisher/sole-finisher Tailscale verification, daily-reset observation, and play-feel tuning against live shop prices.
- **Known Beta listen-host limit:** if peer 1 disconnects, the current ENet session ends rather than migrating authority, so a remaining guest cannot enter the one-finisher Winners Circle. Remote-player departures work now; host-loss settlement belongs with dedicated-server matchmaking or explicit host migration.
- **Winners Circle + Official Beta rewards** (2026-08-21–2026-08-25): the full top-three cinematic/results/Ready flow now banks authenticated Official Classic rewards through a majority-confirmed, idempotent Supabase receipt. Exact XP/Gun Token/Trophy formulas, activity/quit guards, match caps, Level/Trophy roads, three-month Beta Season reset/archive, permanent inventory/tokens, career levels/Prestige, per-mode stats, Match History, and Legacy Hall are implemented and live. Private Tailscale matches count fully when they use the locked rules. The first multi-machine pass exposed a per-client viewer field in the shared reward hash; client and live database canonicalization now strip it, and the disposable three-account settlement regression passes. Remaining follow-up is a repeat real-account Tailscale settlement/play-feel pass, repeated-transition and weaker-laptop profiling, production trophy/road art and animations, and separate team/One of Us Winners Circle designs. Revisit trust/qualification policy when production Edgegap matchmaking replaces Beta listen hosts.
- **Winners Circle modular art blockout** (2026-08-21–2026-08-24): Blender 5.1.2 now generates an editable master plus separate shell, podium, and decor GLBs; Godot assembles those modules around stable top-three, Trophy-drop, ceremonial-gun, confetti, camera, and spotlight anchors. The live result screen uses this authored scene, keeps dynamic podium nameplates and existing result/Ready networking, and has a clean Forward+ rendered checkpoint with real player skins. Fourteen unique production clips now share one dance catalog, remain lazy-loaded on every registered character model, and can each be assigned independently to the Winners Circle slot, Round Win slot, or both. The full swap contract is in `docs/design/WINNERS_CIRCLE_BLOCKOUT.md`. Remaining art work is the production Trophy, room/podium/decor/signage, optional audio mix polish, gun-skin mappings, then cinematic-specific weaker-laptop and repeated-transition profiling.

- **Player Hub and menu organization/presentation pass** (2026-08-24–2026-08-28): the title rail is now the uncluttered Play / Player Hub / Settings / Quit layout with taller cards. Its polished home Player Hub groups Profile, Locker, Prize Counter, and Progression and returns to the Hub after closing a destination; the lobby reuses the portal without Profile. Friends moved to the independent upper-right mascot orb on home/lobby only. Profile has live season/career/mode stats, Legacy Hall, Match History, separate Account/Display Names, 14-day cloud rename, and sign-out. Prize Counter and owned-only Locker share Character / Weapons / Victory / Audio taxonomy, nested filters, full-body dance previews, adaptive shoulder-up Prize Counter Hat previews, top-safe full-body Locker/progression Hat previews, controller focus graphs, dual celebration equip controls, UNEQUIP states, baseline-owned choices, Reset Loadout, and safe art fallbacks. The Locker's always-visible Male Blue Default Cat tile clears both gifted-model and cloud-color overrides; Default Cat, Gold Fish Bag Man, and all future registered character models share one portrait-backed tile layout, with validation requiring a portrait for every model. Player Hub, Profile, Prize Counter, Progression, Locker, Friends, Player Settings, Match Settings, Online Play, reward previews, and full results now omit service breadcrumbs and explanatory developer footers; every window-exit action uses the shared navy bottom-left `BACK` contract. The home/lobby runtime contract, menu/controller validation suite, 24-combination Forward+ Hat render sheet, and 1080p Forward+ title/Hub/Locker/return captures pass. Remaining manual work is physical-controller feel, real-account capture checks, and weaker-laptop confirmation.

- **Supabase identity/cosmetic/progression integration** (2026-08-20–2026-08-29): Auth/session restore, unique Account Names, Gun Tokens, inventory, 13-slot loadout, rotations, favorites/usage, prorated outfit bundles, season/career/profile data, roads/archives, finisher-majority match rewards, the 14-item Victory catalog, ten purchasable Hats, and an entitlement-only character-model slot all use RLS plus authenticated RPCs. Thirteen migrations are live through linked `20260904`; the latest adds five hidden developer-gift character records, the local/remote ledger matches, and the follow-up dry run is clean. Disposable PostgreSQL tests cover exact rewards, different per-client viewer IDs, daily uniqueness, idempotency, and catalog/loadout paths. Rigid wearable sockets plus the skinned-garment binding/compatibility seam now cover Hat, shirt, pants, shoes, and accessory loadout slots without per-frame cosmetic work; Hats are the only mapped art today. No service-role key is present in the project/client. Remaining work is a real gifted-account check, a repeat hands-on real-account Tailscale reward pass, compatible per-model clothing/accessory art plus local registry entries, and final visual mappings for profile badges, weapon skins, and poses. See `docs/SUPABASE.md`.

- **Permanent lower-spec performance baseline** (2026-08-19, laptop checkpoint 2026-08-22, render checkpoint 2026-08-26): `docs/PERFORMANCE.md` is now the standing Forward+ performance contract. Low–Ultra settings propagate render scale/AA/shadow atlases to every viewport; the event-driven quality manager scales screen-space/volumetric/glow/DOF features, local and directional shadows, and particle density without changing gameplay. The lobby keeps its live 3D preview on every tier with scaled rendering and single-scene swaps, Low's main menu uses the lightweight live title world, and `build/.gdignore` prevents generated staging copies from being imported. The current build passed the user-reported weaker-laptop Low-preset compatibility smoke test, and the 2026-08-26 Forward+ matrix passes Player V2, Winners Circle, City, Neon Circuit, settings, accessibility, chat, and all Victory Move captures. Headless startup, settings/scaler/preview, capacity, high-priority, and Godot 4.7 migration validations pass. Remaining performance work is a measured before/after profile at 1080p Low/High, maximum actors/effects, repeated menu/lobby/match transitions, and cinematic-specific laptop retesting.

- **Performance audit watch list** (2026-08-19): profile Forest's roughly 1,025 authored scene nodes / 269 mesh-or-CSG nodes / nine GPU particle systems before any HLOD/MultiMesh refactor; back up and visually validate the used 24.7 MB legacy bot GLB before applying the normal asset optimization pipeline; measure High/Ultra main-menu two-map transition peaks and synchronous gameplay map-switch memory before changing those architectures. Long WAV sources already use Godot compression mode 2 and on-demand loading, so conversion requires runtime evidence rather than source-size guessing.

- **Selected-update launch notes + combat pickup pass** (2026-08-19): release metadata can opt a build into a one-time launch popup with an explicit popup ID, while quiet builds remain manual-only. Sticky Hands now lasts 30s and applies an 8s recollection cooldown after expiration/use, All Gun grants 1s opening and post-hit protection, and a missed Boomerang return grants one second throw before consumption. Neon Circuit is in the map rotation with an interior lobby preview and dedicated thumbnail. Automated coverage exercises the new timing, cooldown, protection, popup-selection, and two-throw behavior.

- **Pixel-art powerup pickups** (2026-08-19–2026-08-27): the seven collectible powerups use cached 64px transparent Sprite3D icons with nearest filtering and per-camera billboarding while preserving their collision, bobbing, world labels, refill timing, and synchronized type changes. Fast Hands now uses the supplied `fastHands2D.png`. Vampire Touch was removed from spawn pools, presets, lobby controls, Playpen supplies, controllers, HUD state, and combat listeners. Old presets safely discard its obsolete registry key during normalization.

- **Title/social/controller/Hat pass complete** (2026-08-27): the taller four-button title rail keeps Profile/Locker/Prize Counter/Progression in Player Hub while Friends moves to a shoulders-up blue-mascot orb on home and online-lobby screens only. Incoming lobby invitations have controller-focusable Accept/Deny toasts; runtime-generated settings, social, store, Locker, and Progression controls now share one idle-button construction with semantic color variants, explicit focus graphs, and pointer-mode focus clearing; and gamepad look has independent X/Y sensitivity. ADS uses a validated shoulder composition that leaves the center lane clear. All 12 supplied Hat models have stable IDs, restored original appearances in budgeted runtime assets, enlarged/seated top-centered fits on both V2 cats, actual fixed-world 3D Prize Counter/Locker previews, and clickable drag/right-stick cosmetic-road previews on the player's selected idle character. Art-pending road rewards use that same centered idle instead of a T-pose. Profile count stats are whole numbers. The online right rail fits all ten roster slots below the independent Friends orb without overlap at 720p, 900p, and 1080p. Migration `20260901` is live: ten hats are visible/purchasable in Prize Counter, Rice Hat is at Beta Level 10, Pimp Hat is at Trophy 5, eligible accounts are backfilled, and live REST verification passes. The client treats SQL-null rotation dates as open-ended, so all ten purchasable hats reach the Prize Counter instead of being discarded by its live-rotation filter. Remaining release QA is physical-controller and weaker-laptop playtesting.

- **Neon Circuit polished prototype** (2026-08-18–2026-08-26): `maps/test/SpaceStationPrototype.tscn` is the registered single-room laser-tag arena. The 38×34 gameplay footprint, cover silhouettes, pickup counts, and exposed central gun remain intact. The scene root stays at its authored 2× scale while a top-level NavigationRegion bakes in world space; the persisted 611-polygon navigation mesh connects all ten spawns without a second inherited scale. Curated CC0 glTF station assets add PBR deck panels, perimeter hardware, columns, pylons, crate shells, wall monitors, vents, and compact ceiling fixtures; the external-asset layer is visual-only and collision-free. The Forward+ validator passes branding, capacity, pickup contracts, art-layer isolation, every spawn/path pair, camera-relative movement, and rendered overview/gameplay views. Hands-on 10-player crossfire, spawn fairness, camera collision, and splitscreen readability testing remain useful rotation checks.
- **Neon Circuit rollback snapshots** (2026-08-18): the exact expanded map immediately before external models remains F6-playable at `maps/test/NeonCircuitPreAssetSnapshot.tscn` with independent navigation at `navigation/NeonCircuitPreAssetNavigation.tres`. The older compact prototype also remains available at `maps/test/SpaceStationPrototypeCompactSnapshot.tscn` with `navigation/SpaceStationPrototypeCompactNavigation.tres`. These snapshots are intentionally unchanged by the polished pass.

- **Edgegap Matchmaker, ticket server, and development coordinator implemented (Phase 11)** (2026-08-14): free Matchmaker `onegun-matchmaker-dev` runs service 3.2.5 against `onegun-dev/dev`. Its generated contract and live two-ticket assignment/public endpoint pass. `match_server_context.gd` plus protocol-2 admission reject invalid tickets and pass two assigned clients through movement/combat. `services/match-coordinator` proxies only allowlisted queue operations with encrypted Worker secrets, signed expiring capabilities, rate limits, protocol checks, and sanitized endpoint output; the shipped Godot client holds no Matchmaker credential. The Cloudflare Worker is deployed, the public client URL is enabled, and two itch clients completed the live ticket-aware Forest gate. A follow-up live test exposed that the dedicated server's fixed invisible melee socket could receive swing requests without reproducing the rendered paw's hit arc; dedicated active windows now sweep the weapon's reach along synchronized aim, and both persistent and match-server two-client suites require a real authoritative melee hit. Rich lobby rules/skins/invites and player authentication remain production backend work.

- **Version mismatch protection complete (Phase 10)** (2026-08-14): client hello now separates player-facing game version, network protocol, and informational commit build ID. New peers remain provisional until the server validates the game/protocol pair; incompatible clients receive a typed rejection, reset their ENet session, never enter the roster, and see a `VERSION MISMATCH` restart/update message. Lobby-full and handshake-timeout errors remain distinct. `tools/run_version_mismatch_smoke.ps1` passes a real two-process ENet rejection without admission, partial roster state, or delayed-disconnect errors. Commit build IDs intentionally remain diagnostic so compatible UI/content-only commits do not force a protocol rejection.

- **Dedicated action/export regression fixed and publicly verified** (2026-08-13): the first automatic Edgegap deployment allowed movement and pickup but gun fire/drop, melee swing, and item activation failed. Logs first exposed a divergent reparented-gun RPC path and stripped presentation resources, then a rendered retry pinpointed the remaining failure: headless pickup removed authoritative objects from the world before resolving a presentation-backed hand socket, orphaning them outside the SceneTree and causing `matching gun not found` / `item ... not found` action rejections. Gun traffic uses the stable `RoundManager` coordinator; dedicated actors now create invisible gameplay-only attachment markers under `AimPivot`; and pickup validates a live socket before reparenting. Build `dev-2318743` passed the automatic GitHub → GHCR → Edgegap deployment and a rendered public client produced authoritative `gun fire requested` logs. The full-resource Linux Docker export also passes controller/guest gun, melee, item, overtime, and online-bot checks. Remaining follow-up is later revalidation of stripped resources after all gameplay/presentation dependencies are separated.
- **Manual Edgegap deployment and public UDP gameplay complete (Phases 4–5)** (2026-08-13): private immutable GHCR image `dev-4868d24-pickupfix1` deploys under `onegun-dev` with verified UDP `24545` and a dynamic external port. A rendered Windows client connected without Tailscale/VPN/port forwarding, became lobby controller, started City with a bot, moved, picked up and used gun/melee/items, and returned to the persistent lobby with matching build IDs. The failed first attempt exposed a real authority bug: roster visibility hid client-owned movement from peer 1 because dedicated servers have no roster entry, leaving host pickup validation at the spawn position. Peer 1 is now always a movement-sync recipient; the two-client dedicated suite asserts remote movement, real input-driven gun pickup, replicated held state, and clean teardown. Phase 6–7 build, publication, registration, and development replacement automation is now verified.
- **GitHub Actions development-server pipeline verified (Phases 6–7)** (2026-08-13): pushes to `main` stamp the commit build ID, install the exact Godot 4.7.1 Linux editor/templates, validate and export the dedicated server, publish an immutable `dev-<short-sha>` GHCR image with the repository-scoped `GITHUB_TOKEN`, update Edgegap alias `onegun-dev/dev`, stop any active deployment whose status exactly matches that development app/version, and deploy the replacement to Ready. Rapid pushes use one cancel-in-progress concurrency group. Edgegap registry/API credentials remain GitHub Secrets. Run `31764060357` deployed build `dev-2318743` and passed rendered public gameplay; production applications and versions are outside the stop policy.
- **External security cleanup still required:** an Edgegap built-in registry push credential was exposed in a setup screenshot. The working pipeline does not use that credential—it uses GHCR with separate temporary write and persistent read-only PATs—but Edgegap support should still revoke/rotate the exposed built-in credential. Never place it or an Edgegap API token in this repository or the client.
- **Build/version metadata complete (Phase 3)** (2026-08-12): `build_info.gd` now separates release version, network protocol, and a generated artifact build ID. Unstamped editor runs use `dev`; `tools/stamp_build.ps1` writes an ignored `build_metadata.json` such as `dev-4868d24`, and both Linux-server and Windows-client exports include it. The server startup line, client admission logs, discovery/lobby metadata, and main-menu footer expose the ID without treating every commit as a protocol break. Focused validation, a two-client dedicated lobby/match/lobby smoke test, Linux export, and a direct exported-PCK launch all passed with the same stamp. The manual Edgegap image/deployment and public UDP gameplay test passed; the next deployment layer is GitHub Actions server automation.
- **Docker dedicated-server layer complete (Phase 2)** (2026-08-12): `Dockerfile.server` packages only the Godot 4.7.1 Linux export in a non-root Debian 12 distroless runtime, publishes ENet UDP 24545, and uses Tini for clean signal forwarding. The allowlist `.dockerignore` excludes the source tree and Windows artifacts. Automated controller/guest validation passed UDP forwarding, synchronized live combat, persistent-lobby recovery, and a 0.41-second exit-code-0 stop. A rendered Windows client joined `127.0.0.1`, became lobby controller, started a City match, and returned to the lobby. Match-scene child ordering and pre-lobby actor teardown prevent the server-side `tree_exiting` spawner cleanup errors exposed by that rendered test.

- **Linux dedicated-server runtime + local verification (Phases 1–1.5)** (2026-08-12): `app_bootstrap.gd` selects normal client startup or an explicit `--server` ENet session; the server contributes no local human, assigns lobby control to the first compatible client, runs match authority headlessly, and returns to its persistent lobby runtime after matches. The existing listen-host and local paths remain available. The Godot 4.7.1 CLI exports a valid Linux x86_64 dedicated executable plus PCK, the three-process server/controller/guest smoke suite reaches live combat cleanly, and a rendered Windows client successfully joined the headless server at `127.0.0.1:24545` with lobby-controller controls. The Docker, build/version, and manual Edgegap layers are verified; the next deployment layer is GitHub Actions server automation.

- **Tierless normalized melee + One of Us loadout** (2026-08-15; retuned 2026-08-17): all five weapon models use one forward-facing 0.45m-radius runtime capsule instead of their differently oriented authored helper shapes. Tiers, tier rolls/scaling, labels, bot preference, and network identity fields are retired. Every mode now uses the same 5.2m length and Reach is the 7.0m exception, with matching host validation. Them retain +15% speed and spawn with the Frying Pan. Fire takes priority over a conflicting Interact binding, and human gun/melee/item entry points require the controller's explicit live Interact transaction, preventing proximity registration from equipping or swapping anything. Melee throws now discard animated-socket root scale, use the compact item flight collider/release, and assign the shared launch velocity deterministically after reparenting. Targeted melee, game-mode, input-remap, pickup-gate, and throw-travel regressions pass; remaining follow-up is hands-on feel testing with real latency.
- **Game modes + The Playpen packet** (2026-08-11; entry/replication fixes 2026-08-17): added One Gun/All Gun/One of Us selection; All Gun personal guns, synchronized depth-tested world heart markers, three-heart sudden death, moving bots, and mode-filtered Extra Life; and the winner-takes-all three-minute One of Us match with role-aware hunter/evader bots, a mode-only dingy grade, conversions/respawns, fixed Us/Them dash counts of 3/4 with the final Us also reaching four, a private volunteer pool with all-roster fallback, and a synchronized 7.35s hunt/transformation cinematic. The Playpen has three fixed six-gun armory bays, correct camera-relative movement, correctly split complete left/right Double Jump Shoes with sole-aligned sockets and one full-height air jump, 2s dedicated refill/respawn and loose-drop cleanup, independent entry/exit with no Ready prompt or mutation, guest-first room opening, and Playpen-only pause actions. Membership reconciliation now refreshes host spawn visibility and each owner's NetSync to prevent locally frozen remote actors; gun pickup requests carry the exact bay instance instead of resolving the first loose gun. The concept-matched lobby uses one Overview/Match Flow/Combat/Spawns/Bots/Presets/Testing Settings rail, the Playpen lighting/material response is substantially less bright and reflective, the bubble-gum splat is 4.5m with a self-owned 5s lifetime, melee throws start clear with explicit thrower immunity, and pause-menu rebinding shares the live-preview/Apply/Cancel behavior of standalone Player Settings. Dedicated local suites and a two-process guest-first loopback test cover all six Playpen guns, bidirectional movement replication, free re-entry, and Ready-state isolation. Remaining follow-up is hands-on camera/game-feel/balance testing, a real multi-machine latency pass, a final One of Us audio cue, and authored Playpen art/audio polish.
- **Returned Playpen lobby input fix** (2026-08-11; prompt removed 2026-08-17): the host's lobby buttons were firing after an independent Playpen exit, but Settings/Character Customization/Player Settings opened on absolute CanvasLayer 20 behind the still-running layer-150 host lobby overlay and appeared to do nothing. Lobby modal layers now resolve relative to their containing canvas. The Playpen validation covers three leave/re-entry cycles in one host session and opens each returned-host modal before immediate re-entry; the two-process smoke exercises the guest's returned-lobby Settings and direct Playpen re-entry while asserting that no confirmation prompt appears and Ready state stays unchanged.
- **Immediate human re-jump** (2026-08-09): removed the hidden 0.4s post-landing jump timer from the shared local/online human controller. A player may jump on the first input after `is_on_floor()` becomes true; the 0.12s uneven-terrain grace remains animation-only and ordinary double-jumping remains unavailable unless a Double Jump Shoes charge is active. Remaining follow-up is hands-on testing on stairs, curbs, and uneven map terrain.
- **Local decoy-shot red blob fixed** (2026-08-09): the 0.5s shooter reveal triggered by destroying a decoy still used the retired expanded, depth-free red shell and could remain stuck on a local gun holder because the cached gun-outline restore returned before clearing it. Human and bot reveal paths now use the scale-independent through-cover surface rim, exclude held equipment, and explicitly remove the temporary overlay before restoring any normal gun-holder presentation. A focused Forward+ capture verifies the live rim and the fully cleared textured model after timeout.
- **City hydrant water launch** (2026-08-09): standing directly in front of Maple & 3rd's bursting hydrant and pressing Jump produces a host-authoritative 15m/s water impulse for humans and bots. After live footage exposed the particle-derived vector pushing sideways, the collision trigger and launch direction were separated: the user's authored box is preserved exactly, and a dedicated marker sends the player straight away from the nozzle. The force blends with entry momentum: perpendicular and with-stream movement is preserved, opposing movement retains 25% before the impulse is added, and the water arc replaces only weaker upward velocity. Opposing air input can no longer erase the shove immediately; until landing or dashing, at least 40% of the water's horizontal stream speed is enforced while lateral steering remains intact. Walking through does nothing, normal 60% air steering otherwise remains available, and dashing replaces the launch. Automated validation covers stationary, head-on, cross-stream and with-stream entry, sustained opposing input, and lateral-drift rejection. Remaining follow-up is hands-on feel tuning against live jumps.
- **Online gun-holder visibility fix** (2026-08-09): the scale-dependent expanded shell that turned the V2 carrier into a solid red blob is replaced by a depth-tested character-surface Fresnel rim, and the overhead `GUN HOLDER` marker is roughly one-third larger with a stronger pulse across online humans, bots, and local per-view presentation. Remaining validation is a real two-machine sightline check at long City distances.
- **Gameplay/visibility packet implemented** (2026-08-06): Reach replaced Magnet Hands; host-synchronized testing hitboxes; dense opaque smoke; 3s cookable 7.5m grenades with falloff; followed-player HUD spectating and revised controls; LOS-gated gun-holder identity; committed-direction spring pads; Flash Camera; first-round authored camera intros; shared confetti/party-favor elimination; and hold-preview/release throw arcs. Godot 4.7.1 editor validation covers script/resource loading. Remaining follow-up is hands-on feel tuning plus real multi-machine network testing, especially Flash Camera framing, smoke readability, spring impulse, intro point composition, and grenade balance.
- **Reach stability + spawn-pool settings follow-up** (2026-08-06; grouping corrected 2026-08-09): Reach's mixed pickup scan now guards type-specific state fields instead of converting absent properties through Godot 4.7.1's invalid `bool(null)` path. The Spawns tab groups all throwable/usable items under Items, only collectible powers under Power Ups, and all five melee types under a minimum-one pool; all choices drive local/online spawn rolls and survive presets/network sync.
- **Movement/visibility tuning follow-up** (2026-08-07; Reach retuned 2026-08-17): spring-pad air control applies a real 60% steering blend for humans and bots, while an airborne dash replaces all remaining pad momentum. Flash Camera begins with 2s of solid white before fading through its distance-scaled 3–6s duration. Reach pickup and powered melee limits are aligned at 7m across local and host-authoritative paths. Remaining follow-up is hands-on feel testing at different frame rates plus a two-machine check of the new 7m pickup/hit limits.
- **V2 human character migration + two-model customization follow-up** (2026-08-08; Female added 2026-08-20): human players, menu/lobby previews, showcase/background actors, and decoys can now use either the Male V2 visual or the rigged `femaleOGCatRigged.glb`; bots intentionally keep their existing model. The two rigs share Mixamo bone/socket names but receive separate target-rest animation caches. Character Customization keeps one rotating preview and adds M/F buttons directly beneath it while preserving the 13-color 5/5/3 grid, P1/P2 pending state, and transactional Cancel/Confirm behavior. Both model and color persist for P1, remain session-only for P2, synchronize through online hello/roster/spawn payloads, appear in gender-specific static portraits, and are inherited by decoys. Focused Godot validation covers the 33-bone female rig, skinned surfaces, runtime Idle/Run/Melee, all 13 textures and portraits, live player/decoy switching, M/F preview isolation, confirm/cancel, and a two-peer female roster update. Remaining hands-on checks are every animation transition and equipment socket on both models in local splitscreen and a real two-machine online match.
- **V2 held-equipment + throw follow-up** (2026-08-08): gun, melee, and item sockets now follow the animated right hand's position while retaining stable character-facing axes, eliminating Mixamo wrist roll that pointed the gun sideways and placed melee assets behind the player. Front/rear rendered captures cover the gun and all five melee types. The gun lowers to the relaxed paw and pitches 55 degrees toward the ground during pistol idle, blends back to its approved forward placement as movement begins, and keeps that normal placement during airborne movement. Throw Object now slices the actual throw/recovery portion of the 4.87s source take instead of its opening toe-touch/pickup motion. Melee weapons and throwable items now share a 15.0 m/s forward plus 5.0 m/s upward launch, with those same values feeding previews and host-authoritative online throws. The expanded disk-free input-remap regression now proves complete P1/P2 controller defaults, primary device filtering, keyboard/mouse and controller replacement bindings, pause/scoreboard/UI access, and distinct splitscreen routing. Remaining checks are feel under live movement/aim transitions and real controller hardware.
- **V2 victory/color hardening** (2026-08-08): victory dance presentation now hides the winner's held gun and restores normal equipment state on respawn. The intermittent texture loss was reproduced as an overlapping hit-flash race: a second flash could capture the first flash's textureless temporary material and restore it permanently. Player and bot flash effects now preserve their source texture, use generation-safe multi-surface restoration, and V2 players explicitly rebind the authoritative selected skin. Automated and rendered regressions apply two rapid flashes to a non-default Salmon player, verify the texture remains, and verify the dance contains no gun.
- **Main-menu/smoke presentation follow-up** (2026-08-08): the podium showcase no longer mounts its display-only gun. Smoke Bomb presentation replaced the solid opaque sphere with a procedural field of opaque inner wisps and animated soft outer wisps. Its tuned gameplay LOS volume is 5m in radius and 4.2m high while retaining the irregular non-dome presentation; bullets remain unaffected. Render validation checks the actual gun-free main menu and smoke opacity against high-contrast scenery.
- **V2 melee grip/swing follow-up** (2026-08-08): Sword, Baseball Bat, Stick, Crowbar, and Frying Pan now use measured model-local handle-base anchors at the paw and normalize their longest held dimension to 1m. Their hidden custom hitboxes inherit the normalized visible scale and anchored transform, removing oversized raw-import hit volumes. The redundant weapon-object rotation tween is gone; the authored V2 melee animation owns presentation while a physics-phase sequence preserves windup, active, recovery, Reach, drop, and throw behavior. Targeted validation exercises all five and the GPU regression captures held front/rear plus active swing poses.

- **Menu Systems packet Phases 1–10 complete** (2026-07-18). The redesign now uses one token/component kit across main menu, local/online lobbies, settings, accessibility/crosshair customization, and context-aware local/host/guest pause cabinets. Personal settings use versioned atomic transactions with live-preview rollback; crosshair/hit/elimination visuals are procedural and local-only, with normalized reload display and validated event/line-of-sight gating. Lobby networking includes ready state, host authority, privacy/share-code metadata, kick, late config sync, and force-start confirmation. Automated resource/runtime, two-peer lobby, and visual capture coverage lives in `tools/menu_systems_validation.gd`, `tools/lobby_network_validation.gd`, and `docs/screenshots/menu_redesign/`. Remaining manual QA is real gamepad hot-plug and two-machine Tailscale playtesting, not missing menu implementation.

- **Grenade and Bubble Gum now use real models** (`Grenade.glb`, `Bubble Gum.glb` under `res://models/items/`), but the detonation itself is still a bare-bones flash effect, not real VFX/SFX. This compounds the existing "no in-match SFX" gap above (a grenade with zero sound is an easy thing to miss).
- **Grenade/Bubble Gum model scale is an estimate**, not visually confirmed — both were scaled down by a flat factor (0.5 and 0.7 respectively) chosen from their raw mesh AABBs to roughly land in a plausible hand-item size range, same approach used for the melee weapon models. Worth a quick look in-editor to confirm they don't look too big/small next to the player, and that neither model is oriented "backwards" the way the baseball bat and sword originally were.
- **Grenade pickup placement on `node_3d.tscn` and `maps/test/NukeTownMap.tscn` is an interpolated guess**, not verified against actual map geometry (placed at the rough midpoint between the two existing item pickups on each map, offset slightly). Open both maps in the editor and confirm the grenade spawn point isn't inside a wall or otherwise unreachable, then reposition to taste.
- **`maps/test/NewMap.tscn` has no item pickups at all** (neither bubble gum trap nor grenade) — wasn't touched, since there was nothing to anchor a placement guess to. Add pickups there if that map is actually in rotation.

## Likely Gaps (inferred from behavior, not commented)

- **Melee kill-feed icon fallback is a hardcoded match statement.** `melee_weapon.gd`'s `_get_weapon_icon()` matches on `weapon_data.weapon_name` string and falls back to 💀 for anything unrecognized — adding a 6th weapon to the registry without updating this function silently loses its icon.
- **Bot line-of-sight check uses `collision_mask = 1`** (world geometry only) — worth verifying bots can't "see"/shoot through other players standing between them and a target, since player collision may be on a different layer (per `project.godot`, layer 2 = "Players").
- **Theme resource is re-saved to disk every launch.** `theme_manager.gd` calls `ResourceSaver.save()` unconditionally in `_ready()` with no existence/diff check — harmless today but worth guarding if theme-building becomes more expensive or this runs somewhere hot.
- **Inventory hold-progress bars use Godot's default `ProgressBar` styling** (`inventory_slots.gd`'s new `HoldBar` nodes) — functional but unstyled; matching the game's cartoon theme (see `theme_manager.gd`'s palette) is a follow-up polish pass, not done yet.

## Housekeeping / Repo Hygiene

- **Git is now initialized** (was not, as of the original pass). ✅
- **`ONEGUN.zip` has been removed from the project root.** ✅
- **Stray Godot editor autosave/temp files still present**: `game_setup.tscn573602726996.tmp`, `game_setup.tscn573634723216.tmp`, `node_3d.tscn557102750531.tmp`, `node_3d.tscn557116189349.tmp`, `node_3d.tscn557231172196.tmp`, `node_3d.tscn81350665721.tmp`. These typically appear after an editor crash or unclean shutdown — worth checking whether any contain unsaved work not present in the committed `.tscn`, then deleting them.
- **Duplicate/stray UID file still present**: `item.gd - Copy.uid` in the project root suggests a copy-paste artifact from a duplicated `item.gd` at some point; confirm there's no orphaned duplicate script and remove the stray `.uid`.
- **`docs/CODING_STANDARDS.md` exists but is empty** — out of scope for this pass (not one of the four requested files), but flagged since it sits alongside the docs just populated.

## Design Decisions Worth Confirming With the Team

- Steam is the intended eventual public transport; keep gameplay/session contracts transport-neutral so replacing the current ENet/Tailscale discovery layer does not require rewriting match authority.
- Is melee-as-disarm-only (rather than melee-as-kill) the intended default forever, or was `melee_eliminates_*` meant to graduate from "house rule" to a first-class game mode selector?
- Bot item use is implemented; ongoing playtests should tune decision intervals and blind-fire risk by difficulty rather than changing bot movement speed.

## Recently Resolved (this session)

- **Godot 4.7.1 migration** (2026-08-05). Disabled the unused Terrain3D 1.0.2 native extension/editor plugin after it crashed during 4.7 initialization; fixed the main-menu error flood, signal-time lobby control deletion, split-camera null access, and the hidden solo P2 viewport continuing to render. Runner paths and v0.0.4 metadata are aligned. Six local maps, targeted validations, export packing, and a rendered D3D12 smoke test pass. Terrain3D must be replaced with a 4.7-compatible upstream build before it is re-enabled.

- **High-priority overhaul items 9–18** (2026-08-01). Unified the ten-active-actor cap and per-map capacity checks; added 2–4-team local/online assignment, uneven-team warnings, grouped scoreboards and friendly fire; separated sequential match actor IDs from network peer IDs; made match settings/presets authoritative and local-only on disk; expanded bot navigation, targets, items, powerups, smoke/fire judgment and time-based difficulty decisions; added 30s online scene-load recovery, five-second compatibility negotiation, late waiting/spectator roles, release notes/version UI, a synchronized three-second lobby launch countdown, and serial local/two-peer/late-spectator validation runners.
- **Pickup supply + lobby follow-up** (2026-08-02). Melee markers now roll a fresh weapon 5s after pickup; item markers roll a fresh enabled item 8s after pickup while the carried original remains usable; powerups re-roll 8s after collection. Local timers are round-generation/OT guarded, and the host gives online replacements fresh stable IDs. The lobby roster is wider and no longer duplicates editable team chips beside team dropdowns. Random Rotation uses a hidden sentinel and mystery preview, so neither host nor clients resolve or reveal the arena until launch.

- **Main-menu mascot: real cat on the trophy pedestal** (2026-07-15; migrated 2026-08-08). The showcase now instances `player.tscn`'s shared V2 human visual, applies P1's saved skin, and requests only the looping Idle clip so menu startup does not load every gameplay FBX. The gold "★ ONE GUN TROPHY ★" Label3D plate remains unchanged; if the named Idle cannot be added, the master model's embedded clip is used instead of a T-pose.

- **UI/HUD "playful arcade" polish pass** (2026-07-13). Foundation: fixed the silent font failure (ThemeManager looked for `Fredoka-Bold.ttf`; the repo ships the variable font) — `FontVariation` weights (500 body / 700 bold) now load, and ThemeManager gained a UI kit (`panel`/`pill`/`heading`/`embolden`/`punch`/`flash`) that every widget and menu routes through. In-match HUD: stamina bar is a rounded track with caption, smooth lerp and an empty-flash/shake; dash pips are rounded cyan panels that pop on refill; powerups render as color-coded pill cards (world-color left bar, self-collapsing description); inventory slots are rounded cards with a gold active border + activation pop; gun/melee/item readouts are bold bottom-right pills ("GUN • READY"/"RELOADING…"); the reload spinner gained a completion ring-flash; the online crosshair follows the player's crosshair color. Feed/banners/scoreboard: kill-feed entries are translucent pills that pop in from the right; the round/set counter sits on a gold-bordered top plate; the TAB scoreboard has a kit panel, gold headers, zebra rows and a gold highlight on your own row online; the online announcement banner slams in per change (white countdown, green GO, gold winners) and the local round label gets the same runtime punch. Menus: main menu gained a vignette + version tag; pause menu is a proper gold-bordered card; player settings scrolls with gold section headers (AUDIO/MOUSE & GAMEPAD/TOGGLES/DISPLAY/CONTROLS); the lobby's floating panels sit on rounded backdrops and Play is a prominent gold button. No map `.tscn` was edited — all restyling is runtime, in the widget scripts.
- **Arena Matchboard overhaul** (2026-08-25). Replaced the generic TAB table with a dedicated live match screen showing official/custom status, match context, round-score pips, placement, kills, disarms, alive state, viewer/host labels and the current One Gun holder. The overlay keeps the arena visible beneath a static scrim and supports FFA/team grouping. Six competitor rows remain visible: the top five plus the viewer's true 6th–10th placement, or the true top six for a top-five viewer. It avoids blur, 3D viewports and continuous animation for Low-spec performance.

- **Playtest feedback round 1** (2026-07-13). Six items from the first multi-friend playtest. **Bugs fixed:** (1) online spectate — the spectator never made its camera current (local play relies on the splitscreen manager, which doesn't exist online), so death froze the view; it now activates follow/free cams online and the player's own camera is reclaimed on round-start respawn; (2) volume sliders — `play_hover` ignored the SFX slider entirely, sliders used a raw dB curve where most of the travel was inaudible (now perceptual squared curve), and the music slider stomped the authored -30 dB base (music played 30 dB louder than designed). **Additions:** Quit button on the main menu; full keybind rebinding screen (Player Settings → Controls) covering move/jump/sprint/dash/interact/fire/throw/ADS/cycle for P1+P2 — keyboard, mouse, and pad buttons rebindable per device class (pad look sticks fixed), persisted in `player_prefs.json` (`input_overrides`) and applied at boot. **Game feel:** crosshair hit-markers (white X + tick on hit, red + lower tick on elimination; gun and melee; works local and online — online emissions are host-confirmed and filtered to the attacker's machine); gun locator — a gold "▼" floats above the loose gun visible through walls, and the current holder gets a red inverted-hull outline (`material_overlay`, depth-tested — walls hide it, no wallhack) on players and bots alike.

- **Phase 2e code review + bot-scale fix** (2026-07-13). Review of the host-owned-bots netcode confirmed the design is sound (actor-ID attribution generalized across gun/melee/item validators, host-only AI with puppet replication, lifecycle handled by the existing `online_actor_state`, disconnect-safe bot ownership). One real bug fixed: online bots never neutralized the map root scale (maps are scale 2), so they rendered — and collided — at twice the size of online humans; `dummy.gd` now applies the same `scale = ONE / map_scale` fix as `character_body_3d.gd`. Minor accepted polish gaps: bots skip the victory dance (no `play_victory_dance` in `dummy.gd`), and `dummy.respawn()` relies on the round flow (not itself) to re-enable physics processing.
- **Online multiplayer Phase 2e — host-owned bots** (2026-07-13; capacity revised 2026-08-01). The online lobby exposes host-only bot count, difficulty, and team assignment through match config, capped at ten active human+bot actors. `RoundManager` spawns stable bot actor IDs from 10000 upward through the existing `MultiplayerSpawner`; only peer 1 runs `dummy.gd` AI/physics, while clients receive synchronized puppet state. Gun, melee, item, powerup, elimination, round reset and scoring paths address actor IDs, allowing bots to use the same host-authoritative validation and scoreboard as humans. Two-process tests cover bot ownership, movement, identity/scoring, weapons, items, and a scored bot round win; real Tailscale verification remains required.
- **Online code review + hardening pass over Phases 2b–2d** (2026-07-13; admission flow superseded 2026-08-01). Coordinator RPC routing, epoch guards, state snapshots and readiness handshakes remain. Later work replaced the former match-time connection refusal with explicit waiting/spectator roles and spawn/RPC visibility filtering. Existing fixes still include boomerang null guards, server-owned lethal protection, actor-ID stat attribution, and debug-only network logging. Remaining accepted limitations include small client visual drift in physics-simulated death drops and no lag prediction/interpolation yet.
- **Online multiplayer Phase 2d — authoritative items + powerups** (2026-07-13; protection names/rules revised 2026-08-01; pickup refill revised 2026-08-02). The host rolls assignments at authored item/powerup markers each round; validates inventory actions; broadcasts final deployed positions; schedules fresh-ID item rerolls 8s after pickup and powerup rerolls 8s after collection; and resolves deployed effects. Powerup collection is synchronized, including authoritative Extra Life and Sticky Hands consumption. The online HUD mirrors local inventory, dash and powerup presentation. Two-process tests cover these actions plus the complete gun/melee/scoring loop; real two-machine Tailscale verification is still required.
- **Online multiplayer Phase 2c — authoritative melee networking** (2026-07-12; marker model revised 2026-08-02). Every authored `melee_spawn_point` is populated at round start. Each successful pickup independently schedules a fresh randomized weapon after 5s with its own stable candidate ID. Pickup, drop, swing, stamina/breaking, throw/landing, death-drop, effects, disarms and optional eliminations remain host-validated and round-epoch guarded through `RoundManager`. Two-process tests pass the authoritative melee loop; two-machine Tailscale verification is still required.
- **Online connection/lobby/leave hardening** (2026-07-13). Host/join closes any previous ENet peer first, dead joins time out and clean up after 12 seconds, and leaving either online screen disconnects explicitly. Hosts choose a lobby name advertised over UDP 24546 to peers returned by the Tailscale CLI; joiners enter that name, with direct `100.x` entry retained as fallback. Each peer can edit only its own synchronized display name in the lobby. The online ESC menu is a local overlay that does not pause replication: only hosts see Return to Lobby, a host Main Menu action removes everyone, and a client's Main Menu action removes only that client. Client-owned authoritative immunity timers now tick on the host, preventing post-melee bullet immunity from becoming permanent; disarm shields remain melee-only. The online lobby preview keeps environmental animation processing active while rendering internally at 960×540. Two-process headless tests pass named discovery, roster renaming, host-coordinated lobby/main transitions, client-only departure, and shield-bypassing bullets; rendered D3D12 lobby and CityMap match tests pass. Real two-machine/Tailscale retesting remains required.
- **Online multiplayer Phase 2b — HUD + round/set/match scoring** (2026-07-12; teams revised 2026-08-01). The host owns countdown/live/ended state, alive actors, actor-keyed stats, round/set wins and match completion. Every round freezes actors, clears stale projectiles, resets the gun, assigns unique spawn markers and respawns active actors before a synchronized countdown. The online HUD provides match state, grouped FFA/team scoreboard, feed, stamina/dashes, inventory/powerups, spectator state and identity tags. Team assignment and friendly fire now work online under the same host-authoritative rules as local play. Two-machine Tailscale verification is still required.
- **Online multiplayer Phase 2a.5 — authoritative-action and scene-readiness hardening** (2026-07-12). Fire/pickup requests are now bound to the actual RPC sender and validated against ownership, alive state, pickup distance, reload state, aim direction and the host's live round epoch. Bullet origins come from the host's gun muzzle; bullets carry their creating round epoch; reload completion is broadcast by the host. `actor_id` is separated from `owner_peer_id` in preparation for bots, online display names use the synchronized peer roster, aim pitch is replicated, and the fixed map-load sleep is replaced by an explicit all-peer scene-ready handshake before spawning. The reusable `tools/run_online_smoke.ps1` two-process test passes readiness, spawn, rejected invalid/stale actions, pickup, reload and elimination on host and client. Two-machine Tailscale verification is still required.
- **Online multiplayer Phase 2a — networked gun combat** (2026-07-12). Host-resolved gun pickup, firing, bullets, eliminations, gun drop and spectating replicate over the network. Death is permanent for the current round; `_net_respawn` is the round-reset hook used by Phase 2b rather than a mid-round auto-respawn. This phase originally stripped melee and other unnetworked systems; Phase 2c subsequently restored melee through its authoritative path. All online paths remain gated behind `NetworkManager.is_online()` so local play is unchanged.
- **Online multiplayer Phase 1 — connect + move together (Tailscale/ENet)** (2026-07-11). New `NetworkManager` autoload: ENet host/join (UDP 24545), peer roster with name exchange (client-pull to dodge the early-connection RPC race), host→client config/map sync (reuses `GameConfig.snapshot_for_preset`), coordinated `change_scene`. "Online" button → Host/Join panel (auto-displays the host's `100.x` Tailscale IP). `game_setup` is host-authoritative online (clients see synced map/settings, host starts). In-map free-roam: `round_manager` runtime-strips baked players/HUD/splitscreen and spawns a networked `player.tscn` per peer via `MultiplayerSpawner`; `character_body_3d` builds a `MultiplayerSynchronizer` (pos/rot/aim/velocity) gated behind `is_online`. **Verified over loopback** (two headless instances): connection, roster+names, config sync, coordinated map load, 2 players spawn on both peers, and remote movement replicates. All gated behind `is_online()` — local play untouched, no map `.tscn` edited. **NOT yet networked (Phase 2):** gun/bullets/melee/items/powerups/eliminations/round-scoring, and online bots.

- **Content mega-batch** (2026-07-11): 5 new items (smoke bomb, bear trap [user models], decoy, boomerang, spring pad [user model]) with deployed scenes/scripts; 5 new powerups (speed surge, silent steps, vampire touch (removed 2026-08-19), second wind, and the former magnet hands, replaced by Reach on 2026-08-06) in both controllers; full combat audio (gunshot, swing, 5 per-weapon hit sounds, stride footsteps — all user-supplied files); marker-driven item/powerup spawning (`item_spawn_point`/`powerup_spawn_point` groups, spawner in `round_manager.gd::_spawn_marker_items`) with all 3 maps surgically converted at the user's exact tuned positions (+2 extra markers each, user repositions at will). All behaviors integration-tested headlessly. See GAME_RULES §7-8.
- **New map: "Maple & 3rd" (`maps/test/CityMap.tscn`)** (2026-07-11). Midday suburban-NY block: cross streets + ring road with markings/crosswalks/curbs, enterable corner store + diner (furnished interiors), brownstone with interior stairs to a walkable roof (verified human-climbable end-to-end; too steep for bot nav — bots reach the interior/landing), park corner reusing forest assets, cars looping with red-light stops + melee-style knockback on contact, cycling traffic lights, bursting hydrant, manhole steam, paper litter, drifting clouds, pigeons. 30 new assets via `tools/gen_city_assets.py`; builder `tools/build_city_map.gd` (RETIRED for rebuilds now that the map is user-tuned - surgical edits only).
- **Maple & 3rd vehicle/streetlight refresh** (2026-08-08): replaced the three traffic visuals with Hatchback, Station Wagon, and Taxi GLBs and all ten lamp visuals with the new Streetlight GLB. Integration wrappers normalize the source assets from 0.2× export scale, ground the cars, and correct their visual forward axes while preserving routes, knockback volumes, authored lamp collisions, and the separately approved TrafficLight GLB. `car_traffic.gd` now discovers both legacy and descriptive wheel node names so all four wheels continue to spin. A Forward+ validation scene checks assignments, dimensions, grounding, wheels, hit zones, untouched traffic lights, and front/rear renders.
- **Items pinned at spawn + 2s return-home** (2026-07-11): items are frozen wherever placed (no roll physics at round start); physics engages only when thrown; dropped items unclaimed for 2s snap back to their spawn point (`item.gd`).
- **Powerups: type fixed on spawn** (2026-07-11): removed the idle 1.5s re-randomizer; type rolls once at spawn and again only when respawning after collection (`powerup.gd`).
- **Car traffic is map-scale-proof** (2026-07-11): waypoints/stop zones now resolve through the parent transform, so scaling a map root no longer derails the routes (`car_traffic.gd`).
- **Step-up trigger hardened** (2026-07-10): low steps (~0.2-0.4m, e.g. brownstone stoops) sit in a dead zone where a capsule registers neither roll-over nor `is_on_wall()`, so the original step-up never fired and bodies stalled. All step-up implementations now scan `get_slide_collision()` normals for steep faces opposing travel instead of relying on the wall flag (`character_body_3d.gd`, `dummy.gd`).
- **Step-up movement for players AND bots** (2026-07-10): both controllers now walk straight over ledges up to 0.55m (knee height) without jumping — `_try_step_up()` shape-casts up→forward→down after `move_and_slide()` and lifts the body only when a walkable surface sits within `STEP_HEIGHT` (steep faces and taller walls still block). Duplicated by hand in `character_body_3d.gd` and `dummy.gd` per the no-shared-base-class convention. Verified: walks over a 0.5m ledge, correctly blocked by a 1.2m wall, and climbs Western v2's stepped tower stairs on pure forward input.
- **Western v2 is now multi-level (MW2019 Gunfight style)** (2026-07-10). Four elevation features, all stairs-accessible and bot-navigable (verified via nav paths + a physics walker crossing the full route with zero stuck frames): walkable rooftops on 3 buildings per row (~3.0-3.8m, parapets = chest-high balcony cover, plank walkways along each row's back lane), a railed catwalk crossing the street directly over the gun spawn with mid-span cover panels, an L-staircase up the (widened) water-tower platform (~5.3m, highest spot, rails on 3 sides), and a +2.2m rock-shelf plateau in the SE quadrant with a ramp onto the SE roof. New models: `StairFlight`, `Catwalk`, `RoofWalkway` in `tools/gen_western_assets.py`. Two real engine-behavior findings baked into the builders: (1) navmesh agent radius must be 0.45 on this map — recast erodes in whole cells and the tower ring needs <0.5m/side; discrete stepped colliders connect far more reliably than thin sloped ramp boxes at floor seams; (2) **fixed a latent bug in BOTH map builders**: the boundary-wall facing formula aligned walls with the ellipse *normal* instead of the tangent, making every invisible wall a ~12.7m radial spike jutting up to ~6m into the map (one sliced through the tower stairs; the forest map was rebuilt with the fix too).
- **New map: "Western v2" (`maps/test/WesternV2Map.tscn`)** (2026-07-10). Sunset main-street showdown town: the gun spawns dead-center of the street (high-noon duel), melee at the church steps. Built with the same pipeline as Whispering Woods — 16 new original Blender models (`tools/gen_western_assets.py` → `models/westernV2/`: 4 false-front storefronts, church, water tower, windmill with separately-hinged spinning blades, hitching posts, troughs, hay bales, sandstone rocks, 2 perimeter mesas, terrain) plus reused assets (the detailed interactive saloon scene — auto-scaled 7.5× from its dollhouse source scale, with wall colliders and a front-door gap so its swinging doors stay usable — clocktower, wagon, crates, barrels, cacti normalized to real heights via measured AABBs, desert backdrop, tumbleweed spawner, bird flock reused as vultures). Atmosphere: golden-hour sun raking down the street, warm volumetric haze, porch lanterns with omni lights, drifting dust motes, spinning windmill (`windmill_spin.gd`). Baked navmesh (805 polys); builder at `tools/build_western_v2.gd` (deterministic, rerunnable via temporary autoload). Registered in the lobby as "Western v2". No audio yet by user's choice.
- **Whispering Woods atmosphere pack** (2026-07-09): wind-sway vertex shader on grass/ferns/flowers/bushes (`wind_sway.gdshader`), pond splash bursts when players wade (`pond_splash.gd`), glowing rune patches on the standing stones (regenerated assets), occasional shooting stars (`shooting_star.gd`), 5 ambient birds (`bird_flock.gd`), and per-level audio: `ForestLevelMusic-Loop.wav` as the match music (music slider) + `ForestBackground-birdChirps.wav` ambience at -10 dB under the SFX slider. `AudioManager` gained a `level_music_key` override (a map claims its own track; `round_manager`'s generic `play_music("game")` gets redirected) and a looping ambient channel. Also fixed: WAVs imported without loop metadata got `loop_end=0` from `_load_looping`, which killed playback instantly — loop mode now set in the two forest `.import` files and `_load_looping` self-heals the empty-loop case for any WAV.
- **Fixed: WASD moved in the wrong direction after spawning on a rotated spawn marker** (2026-07-09, affects all maps). `character_body_3d.gd`'s movement math computes direction from `$AimPivot`'s local basis and assumes the body's yaw is always 0 (mouse look only rotates the pivot) — but `respawn()` stamped the full spawn transform onto the body, so any spawn marker with a nonzero yaw (first introduced by Whispering Woods' facing-center markers) offset the movement axes from the camera. `respawn()` now keeps the body at identity yaw and transfers the marker's yaw to `$AimPivot` instead. Verified via integration test: velocity-to-camera-forward alignment is exactly 1.0 on a rotated marker.
- **New map: "Whispering Woods" (`maps/test/ForestMap.tscn`)** (2026-07-09). Oval twilight fantasy forest arena: center pond crossed by an arched wooden bridge where the gun spawns (fixed mode uses the Gun node's own position; a `gun_spawn_point` marker on the deck also serves random mode), 8 perimeter spawn points, baked navmesh (614 polys) for bots, volumetric-fog twilight WorldEnvironment, moon light, glowshroom OmniLights, falling-leaf + firefly GPUParticles. All ~30 nature models are original low-poly assets generated procedurally in Blender via `tools/gen_forest_assets.py` (rerunnable/tweakable; exports to `models/forestAssets/`, every file KBs — within the CLAUDE.md asset budget). The map itself is rebuilt from scratch by `tools/build_forest_map.gd` (run via temporary autoload) — regenerating either is cheap and deterministic. Registered in the lobby as "Whispering Woods".
- **Editor crashes / extreme lag on `westernMap_saloon.tscn` and `WesternTownMap.tscn` fixed** (2026-07-09). Root cause was NOT Terrain3D: every AI-generated (Tripo) `.glb` in `models/westernAssets/` was ~950k triangles with four 4096×4096 textures (~28-30 MB each; a single bottle carried as much geometry as a AAA character). The saloon scene alone loaded ~10.5M triangles + 2 GB of texture memory. All 21 offenders were batch-optimized in place with glTF Transform (mesh simplify at 0.1% error tolerance, textures → 1024²/2048²): 19.5M → 368k total triangles, 665 MB → 92 MB on disk, import cache 2.3 GB → 232 MB. Pre-optimization originals backed up at `D:\Godot Projects\one-gun_originalAssets_backup\`. Asset budget + pipeline documented in `CLAUDE.md` ("Asset budgets") to prevent recurrence — check any new `.glb` over ~10 MB before adding it.
- **`melee_effects_hit_anyone` is now wired into the lobby UI and preset system** (was previously a code-only flag with no toggle) — resolved when it defaulted to `true`.
- **All 5 melee weapons now have real models with fitted hitboxes** (`sword.tscn`, `baseball_bat.tscn`, `crowbar.tscn`, `frying_pan.tscn`, `stick.tscn`) instead of 4 broken/nonexistent `model_scene_path` references.
- **Pickup/swap/drop overhaul**: tap-to-swap, hold-to-drop, a 3rd inventory slot (2 item slots instead of 1), and the gun-relinquish exception are all implemented and integration-tested. See `docs/ARCHITECTURE.md` §6 and `docs/GAME_RULES.md` §9.
- **Items are now dropped on elimination** (previously only the gun/melee weapon were — items silently vanished with the player).
- **Fixed: displaced items/weapons went invisible on swap.** `gun.gd`/`melee_weapon.gd`/`item.gd`'s `drop()` never reset `visible = true` — since `_update_active_slot_and_visuals()` hides whichever slot isn't currently active, dropping the *inactive* slot's object during a swap (e.g. swapping out an item sitting in the non-active slot) left it invisible on the ground forever, even though it was still fully interactable. All three `drop()` functions now reset visibility; confirmed with a real integration test reproducing the exact reported scenario.

## Recommended Development Priorities

1. **Playtest both V2 human models and the 2026-08-06 gameplay packet** in local splitscreen and with a friend through the verified Edgegap internet path. Confirm collider scale, all animation transitions, gun/melee/item/foot socket offsets, P1/P2 and remote model+color combinations, and owner-matched decoys alongside smoke density, spring strength, Flash Camera framing, grenade falloff, Reach, and authored intro composition.
2. **Profile the reported local-play lag/crash path in a real rendered 4.7.1 session.** Headless/editor validation catches parse/resource faults, but GPU frame timing, imported-map memory, V2 full-animation load time, and play-window crashes need the editor profiler and the exact map/roster that reproduces them.
3. **Resolve the main menu stagger animation** — either restore it or intentionally simplify the code, since a "stripped for debugging" comment left in place is a sign of an interrupted task.
4. **Clean up remaining stray temp/backup files** (`.tmp` autosaves, stray `.uid`) now that git is in place, so the working tree reflects only intentional files.
5. **Audio remains intentionally deprioritized** until requested; keep existing hooks intact but do not let missing final SFX block gameplay/network work.
