# One Gun — TODO / Known Gaps

> Compiled from a full read-through of the codebase on 2026-07-06. Items are grouped by kind, not by priority — see the "Recommended Priorities" section at the bottom for suggested order.

## Explicitly Unfinished (code says so)

- **In-match music is not wired up.** `audio_manager.gd`'s `MUSIC_PATHS["game"]` entry is commented out with an instruction comment to drop a file into `res://audio/` and uncomment it. Menu music works; matches are silent.
- **Most SFX are commented out.** `audio_manager.gd`'s `SFX_PATHS` has entries stubbed for gun pickup/drop, disarm, round start/end, death — all commented out, no audio files present. Only UI hover/click sounds are live.
- **Main menu button stagger animation was gutted for debugging.** `main_menu.gd`'s `_stagger_page_buttons()` has a comment stating the slide/stagger animation was "stripped for debugging" — buttons fade in but no longer slide/stagger as apparently originally designed. Needs restoring or intentionally removing the dead code path.
- **Online Phase 3 polish remains.** The full Phase 1-2e match stack is implemented. Remaining online work is latency smoothing/prediction, reconnect/host-left polish, and real two-machine Tailscale playtesting.
- **Player 2's name is session-only.** `GameConfig.player2_name` is explicitly commented as "session-only, not saved to disk — P2 may be a different guest each time." This looks intentional (P2 is a rotating local guest) but is worth confirming with design intent rather than assuming it's an oversight.

## Recently Added, Needs Follow-up

- **Grenade and Bubble Gum now use real models** (`Grenade.glb`, `Bubble Gum.glb` under `res://models/items/`), but the detonation itself is still a bare-bones flash effect, not real VFX/SFX. This compounds the existing "no in-match SFX" gap above (a grenade with zero sound is an easy thing to miss).
- **Grenade/Bubble Gum model scale is an estimate**, not visually confirmed — both were scaled down by a flat factor (0.5 and 0.7 respectively) chosen from their raw mesh AABBs to roughly land in a plausible hand-item size range, same approach used for the melee weapon models. Worth a quick look in-editor to confirm they don't look too big/small next to the player, and that neither model is oriented "backwards" the way the baseball bat and sword originally were.
- **Grenade pickup placement on `node_3d.tscn` and `maps/test/NukeTownMap.tscn` is an interpolated guess**, not verified against actual map geometry (placed at the rough midpoint between the two existing item pickups on each map, offset slightly). Open both maps in the editor and confirm the grenade spawn point isn't inside a wall or otherwise unreachable, then reposition to taste.
- **`maps/test/NewMap.tscn` has no item pickups at all** (neither bubble gum trap nor grenade) — wasn't touched, since there was nothing to anchor a placement guess to. Add pickups there if that map is actually in rotation.
- **Grenade knockback is a flat 4.0 with no distance falloff** within its 6m radius — someone at the very edge gets knocked exactly as hard as someone standing on top of it. Worth playtesting; falloff was intentionally deferred rather than guessed at.

## Likely Gaps (inferred from behavior, not commented)

- **No player cosmetics/customization system**, despite the settings UI existing for controls/audio/FOV — there's no model color, outfit, or skin selection anywhere. If a "Customize Your Character" entry point exists in menu copy/design intent, it currently has no backing implementation.
- **Melee kill-feed icon fallback is a hardcoded match statement.** `melee_weapon.gd`'s `_get_weapon_icon()` matches on `weapon_data.weapon_name` string and falls back to 💀 for anything unrecognized — adding a 6th weapon to the registry without updating this function silently loses its icon.
- **Bot line-of-sight check uses `collision_mask = 1`** (world geometry only) — worth verifying bots can't "see"/shoot through other players standing between them and a target, since player collision may be on a different layer (per `project.godot`, layer 2 = "Players").
- **Theme resource is re-saved to disk every launch.** `theme_manager.gd` calls `ResourceSaver.save()` unconditionally in `_ready()` with no existence/diff check — harmless today but worth guarding if theme-building becomes more expensive or this runs somewhere hot.
- **Inventory hold-progress bars use Godot's default `ProgressBar` styling** (`inventory_slots.gd`'s new `HoldBar` nodes) — functional but unstyled; matching the game's cartoon theme (see `theme_manager.gd`'s palette) is a follow-up polish pass, not done yet.

## Housekeeping / Repo Hygiene

- **Git is now initialized** (was not, as of the original pass). ✅
- **`ONEGUN.zip` has been removed from the project root.** ✅
- **Stray Godot editor autosave/temp files still present**: `game_setup.tscn573602726996.tmp`, `game_setup.tscn573634723216.tmp`, `node_3d.tscn557102750531.tmp`, `node_3d.tscn557116189349.tmp`, `node_3d.tscn557231172196.tmp`, `node_3d.tscn81350665721.tmp`. These typically appear after an editor crash or unclean shutdown — worth checking whether any contain unsaved work not present in the committed `.tscn`, then deleting them.
- **Duplicate/stray UID file still present**: `item.gd - Copy.uid` in the project root suggests a copy-paste artifact from a duplicated `item.gd` at some point; confirm there's no orphaned duplicate script and remove the stray `.uid`.
- **`docs/CODING_STANDARDS.md` exists but is empty** — out of scope for this pass (not one of the four requested files), but flagged since it sits alongside the docs just populated.

## Design Decisions Worth Confirming With the Team

- Should online play be attempted at all, or is "local-only" a permanent design pillar? This affects whether the disabled menu button should be removed entirely or left as a future placeholder.
- Is melee-as-disarm-only (rather than melee-as-kill) the intended default forever, or was `melee_eliminates_*` meant to graduate from "house rule" to a first-class game mode selector?
- Should bots eventually use items, or is that a deliberate "humans only" hazard-balance choice?

## Recently Resolved (this session)

- **Phase 2e code review + bot-scale fix** (2026-07-13). Review of the host-owned-bots netcode confirmed the design is sound (actor-ID attribution generalized across gun/melee/item validators, host-only AI with puppet replication, lifecycle handled by the existing `online_actor_state`, disconnect-safe bot ownership). One real bug fixed: online bots never neutralized the map root scale (maps are scale 2), so they rendered — and collided — at twice the size of online humans; `dummy.gd` now applies the same `scale = ONE / map_scale` fix as `character_body_3d.gd`. Minor accepted polish gaps: bots skip the victory dance (no `play_victory_dance` in `dummy.gd`), and `dummy.respawn()` relies on the round flow (not itself) to re-enable physics processing.
- **Online multiplayer Phase 2e — host-owned bots** (2026-07-13). The online lobby exposes host-only bot count and per-bot difficulty, synchronized through the existing match config and capped at eight total human+bot actors. `RoundManager` spawns stable bot actor IDs through the existing `MultiplayerSpawner`; only peer 1 runs `dummy.gd` AI/physics, while clients receive position/rotation/velocity/stamina and puppet animation. Gun, melee, item, powerup, elimination, round reset and scoring paths now address actor IDs, allowing bots to use the same host-authoritative validation and scoreboard as humans. No map scene or authored marker was edited. Two-process tests pass bot ownership, movement, name/score state, gun fire, melee swing, item pickup and a scored bot round win on all three selectable maps; real Tailscale verification remains required.
- **Online code review + hardening pass over Phases 2b–2d** (2026-07-13). Full review of the phase 2b–2d netcode; architecture kept intact (coordinator RPC routing, epoch guards, state snapshots, readiness handshake all confirmed sound). Six fixes applied: (1) the host now refuses new ENet connections during a match and reopens them on lobby return (`NetworkManager.set_accepting_new_peers`) — previously a late joiner via lobby discovery would strand in the lobby while the spawner errored trying to replicate to them; (2) null-guard on the boomerang consume path in `server_online_boomerang_hit` (host crash if the item was replaced the same frame); (3) the local Second-Wind branch in `character_body_3d.eliminate()` is offline-only — online the server resolves Second Wind, and the local branch could desync a diverged peer; (4) online gun-pickup stats attribute by the gun holder's `actor_id` instead of display name (same-name players mis-credited); (5) Magnet Hands item moves stream over an `unreliable_ordered` RPC instead of the reliable combat channel; (6) `_net_log` prints only in debug builds. **Accepted limitations (not bugs):** death-drops land at each peer's local copy of the victim position (few-cm drift; melee re-syncs via the despawn timer, gun doesn't), no server-side proximity check on melee/item pickups (gun has one; powerups check 3 m), no lag interpolation/prediction yet (Phase 3).
- **Online multiplayer Phase 2d — authoritative items + powerups** (2026-07-13). Every authored item, powerup, player-spawn and melee placement remains untouched in the map scenes. The host rolls item/powerup assignments at the existing markers each round; validates two-slot pickup/swap/drop/throw actions; broadcasts final deployed positions; schedules marker-preserving rerolls; and resolves grenade, bubble-gum, bear-trap, spring-pad, decoy, boomerang and Magnet Hands effects. Powerup collection is synchronized, with authoritative Second Wind and disarm-shield consumption. The online HUD uses the local three-slot inventory presentation (including equipped-slot highlighting and drop-hold progress), matching stamina-width dash pips, and active-powerup timers. Two-process tests pass Phase 2d actions plus the complete gun/melee/scoring loop on Whispering Woods, Western Town and Maple & 3rd; the rendered D3D12 Maple & 3rd run passes on both peers. Real two-machine Tailscale verification is still required.
- **Online multiplayer Phase 2c — authoritative melee networking** (2026-07-12, multi-placement correction 2026-07-13). Every authored melee placement remains in the map and is active online. The host assigns each placement a stable ID and independently rolls and synchronizes its weapon type/effect/tier every round. Pickup, voluntary drop, swing, stamina cost/deficit breaking, throw/landing, death-drop despawn, hit effects, gun disarm/reacquisition lock and optional melee eliminations are host-validated and round-epoch guarded. Reparent-sensitive weapon traffic routes through the stable `RoundManager` RPC path and includes the placement ID. Online HUD/scoreboard show the held melee identity and synchronized melee-hit/disarm stats. The default hitbox's unsupported Jolt non-uniform scale was baked into its shape dimensions, eliminating the spawn warning without changing its world-space size. Two-process headless and rendered tests pass the full loop on CityMap, including activation and identity validation for all seven authored placements; two-machine Tailscale verification is still required.
- **Online connection/lobby/leave hardening** (2026-07-13). Host/join closes any previous ENet peer first, dead joins time out and clean up after 12 seconds, and leaving either online screen disconnects explicitly. Hosts choose a lobby name advertised over UDP 24546 to peers returned by the Tailscale CLI; joiners enter that name, with direct `100.x` entry retained as fallback. Each peer can edit only its own synchronized display name in the lobby. The online ESC menu is a local overlay that does not pause replication: only hosts see Return to Lobby, a host Main Menu action removes everyone, and a client's Main Menu action removes only that client. Client-owned authoritative immunity timers now tick on the host, preventing post-melee bullet immunity from becoming permanent; disarm shields remain melee-only. The online lobby preview keeps environmental animation processing active while rendering internally at 960×540. Two-process headless tests pass named discovery, roster renaming, host-coordinated lobby/main transitions, client-only departure, and shield-bypassing bullets; rendered D3D12 lobby and CityMap match tests pass. Real two-machine/Tailscale retesting remains required.
- **Online multiplayer Phase 2b — HUD + round/set/match scoring** (2026-07-12). The host now owns countdown/live/ended state, alive actors, kills/deaths/pickups, round wins, set wins and match completion. Every round freezes actors, clears bullets, resets the single gun, assigns spawn markers and respawns all actors before a synchronized countdown; stale-epoch combat is rejected. A new one-per-machine `online_hud.gd` binds to the locally owned actor and provides round/first-to/set awareness, large countdown/winner banners, alive count, TAB scoreboard, kill feed, stamina, dash charges, ammo/reload, spectator state and remote roster-backed name tags. Match completion returns all peers to the still-connected online lobby. Online scoring is deliberately FFA for now; team/friendly-fire settings are labeled offline-only. The two-process smoke test passes two scored rounds, reset/respawn, synchronized stats, a set/match win, HUD binding on both peers and coordinated lobby return. Two-machine Tailscale verification is still required.
- **Online multiplayer Phase 2a.5 — authoritative-action and scene-readiness hardening** (2026-07-12). Fire/pickup requests are now bound to the actual RPC sender and validated against ownership, alive state, pickup distance, reload state, aim direction and the host's live round epoch. Bullet origins come from the host's gun muzzle; bullets carry their creating round epoch; reload completion is broadcast by the host. `actor_id` is separated from `owner_peer_id` in preparation for bots, online display names use the synchronized peer roster, aim pitch is replicated, and the fixed map-load sleep is replaced by an explicit all-peer scene-ready handshake before spawning. The reusable `tools/run_online_smoke.ps1` two-process test passes readiness, spawn, rejected invalid/stale actions, pickup, reload and elimination on host and client. Two-machine Tailscale verification is still required.
- **Online multiplayer Phase 2a — networked gun combat** (2026-07-12). Host-resolved gun pickup, firing, bullets, eliminations, gun drop and spectating replicate over the network. Death is permanent for the current round; `_net_respawn` is the round-reset hook used by Phase 2b rather than a mid-round auto-respawn. This phase originally stripped melee and other unnetworked systems; Phase 2c subsequently restored melee through its authoritative path. All online paths remain gated behind `NetworkManager.is_online()` so local play is unchanged.
- **Online multiplayer Phase 1 — connect + move together (Tailscale/ENet)** (2026-07-11). New `NetworkManager` autoload: ENet host/join (UDP 24545), peer roster with name exchange (client-pull to dodge the early-connection RPC race), host→client config/map sync (reuses `GameConfig.snapshot_for_preset`), coordinated `change_scene`. "Online" button → Host/Join panel (auto-displays the host's `100.x` Tailscale IP). `game_setup` is host-authoritative online (clients see synced map/settings, host starts). In-map free-roam: `round_manager` runtime-strips baked players/HUD/splitscreen and spawns a networked `player.tscn` per peer via `MultiplayerSpawner`; `character_body_3d` builds a `MultiplayerSynchronizer` (pos/rot/aim/velocity) gated behind `is_online`. **Verified over loopback** (two headless instances): connection, roster+names, config sync, coordinated map load, 2 players spawn on both peers, and remote movement replicates. All gated behind `is_online()` — local play untouched, no map `.tscn` edited. **NOT yet networked (Phase 2):** gun/bullets/melee/items/powerups/eliminations/round-scoring, and online bots.

- **Content mega-batch** (2026-07-11): 5 new items (smoke bomb, bear trap [user models], decoy, boomerang, spring pad [user model]) with deployed scenes/scripts; 5 new powerups (speed surge, silent steps, vampire touch, second wind, magnet hands) in both controllers; full combat audio (gunshot, swing, 5 per-weapon hit sounds, stride footsteps — all user-supplied files); marker-driven item/powerup spawning (`item_spawn_point`/`powerup_spawn_point` groups, spawner in `round_manager.gd::_spawn_marker_items`) with all 3 maps surgically converted at the user's exact tuned positions (+2 extra markers each, user repositions at will). All behaviors integration-tested headlessly. See GAME_RULES §7-8.
- **New map: "Maple & 3rd" (`maps/test/CityMap.tscn`)** (2026-07-11). Midday suburban-NY block: cross streets + ring road with markings/crosswalks/curbs, enterable corner store + diner (furnished interiors), brownstone with interior stairs to a walkable roof (verified human-climbable end-to-end; too steep for bot nav — bots reach the interior/landing), park corner reusing forest assets, cars looping with red-light stops + melee-style knockback on contact, cycling traffic lights, bursting hydrant, manhole steam, paper litter, drifting clouds, pigeons. 30 new assets via `tools/gen_city_assets.py`; builder `tools/build_city_map.gd` (RETIRED for rebuilds now that the map is user-tuned - surgical edits only).
- **Items pinned at spawn + 2s return-home** (2026-07-11): items are frozen wherever placed (no roll physics at round start); physics engages only when thrown; dropped items unclaimed for 2s snap back to their spawn point (`item.gd`).
- **Powerups: type fixed on spawn** (2026-07-11): removed the idle 1.5s re-randomizer; type rolls once at spawn and again only when respawning after collection (`powerup.gd`).
- **Car traffic is map-scale-proof** (2026-07-11): waypoints/stop zones now resolve through the parent transform, so scaling a map root no longer derails the routes (`car_traffic.gd`).
- **Step-up trigger hardened** (2026-07-10): low steps (~0.2-0.4m, e.g. brownstone stoops) sit in a dead zone where a capsule registers neither roll-over nor `is_on_wall()`, so the original step-up never fired and bodies stalled. All step-up implementations now scan `get_slide_collision()` normals for steep faces opposing travel instead of relying on the wall flag (`character_body_3d.gd`, `dummy.gd`).
- **Step-up movement for players AND bots** (2026-07-10): both controllers now walk straight over ledges up to 0.55m (knee height) without jumping — `_try_step_up()` shape-casts up→forward→down after `move_and_slide()` and lifts the body only when a walkable surface sits within `STEP_HEIGHT` (steep faces and taller walls still block). Duplicated by hand in `character_body_3d.gd` and `dummy.gd` per the no-shared-base-class convention. Verified: walks over a 0.5m ledge, correctly blocked by a 1.2m wall, and climbs Western v2's stepped tower stairs on pure forward input.
- **Western v2 is now multi-level (MW2019 Gunfight style)** (2026-07-10). Four elevation features, all stairs-accessible and bot-navigable (verified via nav paths + a physics walker crossing the full route with zero stuck frames): walkable rooftops on 3 buildings per row (~3.0-3.8m, parapets = chest-high balcony cover, plank walkways along each row's back lane), a railed catwalk crossing the street directly over the gun spawn with mid-span cover panels, an L-staircase up the (widened) water-tower platform (~5.3m, highest spot, rails on 3 sides), and a +2.2m rock-shelf plateau in the SE quadrant with a ramp onto the SE roof. New models: `StairFlight`, `Catwalk`, `RoofWalkway` in `tools/gen_western_assets.py`. Two real engine-behavior findings baked into the builders: (1) navmesh agent radius must be 0.45 on this map — recast erodes in whole cells and the tower ring needs <0.5m/side; discrete stepped colliders connect far more reliably than thin sloped ramp boxes at floor seams; (2) **fixed a latent bug in BOTH map builders**: the boundary-wall facing formula aligned walls with the ellipse *normal* instead of the tangent, making every invisible wall a ~12.7m radial spike jutting up to ~6m into the map (one sliced through the tower stairs; the forest map was rebuilt with the fix too).
- **New map: "Western v2" (`maps/test/WesternV2Map.tscn`)** (2026-07-10). Sunset main-street showdown town: the gun spawns dead-center of the street (high-noon duel), melee at the church steps. Built with the same pipeline as Whispering Woods — 16 new original Blender models (`tools/gen_western_assets.py` → `models/westernV2/`: 4 false-front storefronts, church, water tower, windmill with separately-hinged spinning blades, hitching posts, troughs, hay bales, sandstone rocks, 2 perimeter mesas, terrain) plus reused assets (the detailed interactive saloon scene — auto-scaled 7.5× from its dollhouse source scale, with wall colliders and a front-door gap so its swinging doors stay usable — clocktower, wagon, crates, barrels, cacti normalized to real heights via measured AABBs, desert backdrop, tumbleweed spawner, bird flock reused as vultures). Atmosphere: golden-hour sun raking down the street, warm volumetric haze, porch lanterns with omni lights, drifting dust motes, spinning windmill (`windmill_spin.gd`). Baked navmesh (556 polys); builder at `tools/build_western_v2.gd` (deterministic, rerunnable via temporary autoload). Registered in the lobby as "Western v2". No audio yet by user's choice.
- **Whispering Woods atmosphere pack** (2026-07-09): wind-sway vertex shader on grass/ferns/flowers/bushes (`wind_sway.gdshader`), pond splash bursts when players wade (`pond_splash.gd`), glowing rune patches on the standing stones (regenerated assets), occasional shooting stars (`shooting_star.gd`), 5 ambient birds (`bird_flock.gd`), and per-level audio: `ForestLevelMusic-Loop.wav` as the match music (music slider) + `ForestBackground-birdChirps.wav` ambience at -10 dB under the SFX slider. `AudioManager` gained a `level_music_key` override (a map claims its own track; `round_manager`'s generic `play_music("game")` gets redirected) and a looping ambient channel. Also fixed: WAVs imported without loop metadata got `loop_end=0` from `_load_looping`, which killed playback instantly — loop mode now set in the two forest `.import` files and `_load_looping` self-heals the empty-loop case for any WAV.
- **Fixed: WASD moved in the wrong direction after spawning on a rotated spawn marker** (2026-07-09, affects all maps). `character_body_3d.gd`'s movement math computes direction from `$AimPivot`'s local basis and assumes the body's yaw is always 0 (mouse look only rotates the pivot) — but `respawn()` stamped the full spawn transform onto the body, so any spawn marker with a nonzero yaw (first introduced by Whispering Woods' facing-center markers) offset the movement axes from the camera. `respawn()` now keeps the body at identity yaw and transfers the marker's yaw to `$AimPivot` instead. Verified via integration test: velocity-to-camera-forward alignment is exactly 1.0 on a rotated marker.
- **New map: "Whispering Woods" (`maps/test/ForestMap.tscn`)** (2026-07-09). Oval twilight fantasy forest arena: center pond crossed by an arched wooden bridge where the gun spawns (fixed mode uses the Gun node's own position; a `gun_spawn_point` marker on the deck also serves random mode), 8 perimeter spawn points, baked navmesh (569 polys) for bots, volumetric-fog twilight WorldEnvironment, moon light, glowshroom OmniLights, falling-leaf + firefly GPUParticles. All ~30 nature models are original low-poly assets generated procedurally in Blender via `tools/gen_forest_assets.py` (rerunnable/tweakable; exports to `models/forestAssets/`, every file KBs — within the CLAUDE.md asset budget). The map itself is rebuilt from scratch by `tools/build_forest_map.gd` (run via temporary autoload) — regenerating either is cheap and deterministic. Registered in the lobby as "Whispering Woods".
- **Editor crashes / extreme lag on `westernMap_saloon.tscn` and `WesternTownMap.tscn` fixed** (2026-07-09). Root cause was NOT Terrain3D: every AI-generated (Tripo) `.glb` in `models/westernAssets/` was ~950k triangles with four 4096×4096 textures (~28-30 MB each; a single bottle carried as much geometry as a AAA character). The saloon scene alone loaded ~10.5M triangles + 2 GB of texture memory. All 21 offenders were batch-optimized in place with glTF Transform (mesh simplify at 0.1% error tolerance, textures → 1024²/2048²): 19.5M → 368k total triangles, 665 MB → 92 MB on disk, import cache 2.3 GB → 232 MB. Pre-optimization originals backed up at `D:\Godot Projects\one-gun_originalAssets_backup\`. Asset budget + pipeline documented in `CLAUDE.md` ("Asset budgets") to prevent recurrence — check any new `.glb` over ~10 MB before adding it.
- **`melee_effects_hit_anyone` is now wired into the lobby UI and preset system** (was previously a code-only flag with no toggle) — resolved when it defaulted to `true`.
- **All 5 melee weapons now have real models with fitted hitboxes** (`sword.tscn`, `baseball_bat.tscn`, `crowbar.tscn`, `frying_pan.tscn`, `stick.tscn`) instead of 4 broken/nonexistent `model_scene_path` references.
- **Pickup/swap/drop overhaul**: tap-to-swap, hold-to-drop, a 3rd inventory slot (2 item slots instead of 1), and the gun-relinquish exception are all implemented and integration-tested. See `docs/ARCHITECTURE.md` §6 and `docs/GAME_RULES.md` §9.
- **Items are now dropped on elimination** (previously only the gun/melee weapon were — items silently vanished with the player).
- **Fixed: displaced items/weapons went invisible on swap.** `gun.gd`/`melee_weapon.gd`/`item.gd`'s `drop()` never reset `visible = true` — since `_update_active_slot_and_visuals()` hides whichever slot isn't currently active, dropping the *inactive* slot's object during a swap (e.g. swapping out an item sitting in the non-active slot) left it invisible on the ground forever, even though it was still fully interactable. All three `drop()` functions now reset visibility; confirmed with a real integration test reproducing the exact reported scenario.

## Recommended Development Priorities

1. **Wire up core audio feedback** (gunshot, hit, disarm, elimination, round-start/end SFX + in-match music). Right now combat has zero audio feedback beyond menu sounds, which is likely the single biggest gap between "functionally complete" and "feels good to play." Low code risk — mostly asset integration into an existing, working `AudioManager`.
2. **Resolve the main menu stagger animation** — either restore it or intentionally simplify the code, since a "stripped for debugging" comment left in place is a sign of an interrupted task, and it's very low effort to close out.
3. **Decide and implement (or explicitly close out) bot item usage** — this is a small, contained AI behavior addition if it's wanted, and worth resolving before it's forgotten.
4. **Clean up remaining stray temp/backup files** (`.tmp` autosaves, stray `.uid`) now that git is in place, so the working tree reflects only intentional files.
5. **Only after the above**: consider new-feature work (cosmetics, online play, additional weapons/maps) — these are larger scope and better tackled once the existing systems are polished.
